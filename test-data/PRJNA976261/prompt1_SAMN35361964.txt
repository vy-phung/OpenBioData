Prompt 1: Given the following text snippets, analyze the biological sample with accession number SAMN35361964.
Identify its primary associated geographic location (country preferred; fall back to region/continent if no country mentioned; write 'unknown' if no geographic clues are present).
Determine if the sample source is 'modern' (living individual) or 'ancient' (prehistoric/archaeological); assume 'modern' if not specified.
Extract the following metadata fields: study_name, subject_id, sample_id, target_condition, control, body_site, sequencing_platform, host_species.
IMPORTANT — per-subject assignment: many studies have multiple participant groups (e.g. a reference/control group and one or more affected/exposed groups). Search for a table or supplementary file that maps individual sample IDs / subject IDs / NCBI BioSample accessions to their specific group. The accession being analysed is shown in 'Prompt N:' above — find its row in that table and extract the group/condition for THAT SPECIFIC SAMPLE, not the study as a whole. IF the source text contains a 'Pre-computed table reliability' block: that determination has already been made for you mechanically -- use ONLY the table(s) it marks RELIABLE for this lookup, and do not re-judge or override it, even if an UNRELIABLE table looks more detailed or convincing. Skip the PRIORITY RULE/RELIABILITY TEST/WORKED EXAMPLE guidance below in that case -- it only applies when no such pre-computed block is present. PRIORITY RULE: a table/section that maps individual sample identifiers to their specific category is stronger evidence than general prose describing the study's groups or conditions as a whole; when such a table/section exists and is RELIABLE (see below), cite its matching row, not the prose. COMPLETENESS: do not stop at the first candidate table/section you find. First check what identifying attribute(s) actually exist on this sample's own record, whatever they are called (id, subject_id, isolate_name, strain, specimen_code, patient number, or anything else) -- then check EVERY table/section in the source text that uses a matching or clearly related identifying scheme, not just the first or most prominent one. RELIABILITY TEST (structural -- does not depend on how the identifier is named or numbered): a candidate table/section is reliable ONLY if it maps each identifier to a value either (a) as an explicit, unique row per identifier with no identifier repeated, or (b) as a non-overlapping range/bucket that unambiguously covers the identifier (one bucket per contiguous span, no overlap). Both forms guarantee exactly one answer per identifier, regardless of whether the identifier is numeric, a name, or a code. A scheme where the same identifier value recurs across multiple sections/sub-lists (e.g. numbering that restarts within each sub-group) qualifies under NEITHER form and is NOT reliable, no matter how prominent, detailed, or narratively rich that section is. When multiple candidates exist, ALWAYS prefer a reliable one over an unreliable one, even if the unreliable one appears earlier or is more prominent in the text. If NO candidate qualifies as reliable under either form, output 'unknown' for this field rather than guessing which section's row applies. WORKED EXAMPLE (fictional, illustrative only -- does not describe any real study): suppose one candidate, Table A, lists subjects as 'Group X: #1, #2, #3' and 'Group Y: #1, #2, #3' (the numbers 1-3 recur in both groups) -- this is NOT reliable, since an identifier of '2' could mean Group X's 2nd subject OR Group Y's 2nd subject. A second candidate, Table B, is a single continuous list: 'Sample 1: Group X; Sample 2: Group Y; Sample 3: Group X; Sample 4: Group Y; Sample 5: Group X; Sample 6: Group Y' with no repeated sample numbers -- this IS reliable, since each identifier appears exactly once. If the subject being analysed has identifier '2', the correct answer comes from Table B's row for identifier 2 (Group Y), NOT from either of Table A's '#2' entries -- even if Table A appeared earlier or contained more narrative detail. Table B is chosen because it guarantees exactly one answer per identifier; Table A does not. REQUIRED FORMAT for categorical/group-type fields: before giving your final answer, you MUST first write '[Candidates: <name1>=<reliable|unreliable> (<short reason>); <name2>=<reliable|unreliable> (<short reason>); ...]' listing EVERY candidate table/section you found (write '[Candidates: none found]' if none exist), then '[Chosen: <name of the one actually used, or none>]', THEN your narrative sentence citing that chosen source. Do this even if there is only one candidate -- list it and judge it anyway; do not skip this step. Check NCBI BioSample attributes (e.g. 'disease', 'health_state', 'clinical_diagnosis', 'group', 'treatment', 'subject_group') and also paper tables / supplementary metadata tables. Do NOT report the full list of study groups — report only the group for this individual sample. CONTROL DEFINITION: a 'control' sample belongs to the group with NONE of the study's conditions/exposures present (the fully unaffected/reference group) -- not merely 'not the primary condition being studied.' If you cannot confidently determine full-unaffected status for this sample, output 'unknown' rather than defaulting to a case/disease label.
IMPORTANT — for study/dataset-identifier fields: use the paper's own identifying convention (e.g. first-author surname + publication year, such as 'SmithJ_2021'), or the actual publication title if no such convention is stated. Do NOT output an NCBI/ENA accession number (BioProject, BioSample, SRA/ENA study ID, run accession, etc.) as the answer -- an accession-shaped string is never a valid study/dataset name.
For each field: find the most specific value stated anywhere in the sources.
Infer from context when not explicit. Write 'unknown' ONLY when truly absent from all sources.
If different sources give DIFFERENT values for the same field, keep the most specific/reliable
value AND append '##CONFLICT: source_A=<val_A>, source_B=<val_B>' to that value so the conflict is visible.

OUTPUT FORMAT (follow exactly — no markdown, no extra headers):
Line 1: exactly 10 values for: country_name, modern/ancient/unknown, study_name, subject_id, sample_id, target_condition, control, body_site, sequencing_platform, host_species, separated by ' | ' (space-pipe-space).
  IMPORTANT: use ' | ' ONLY as the field separator. Within a single field value, use commas freely.
  For conflicting values within one field write: <best_value> ##CONFLICT: source_A=<val_A>, source_B=<val_B>
Lines 2–11: one line per field in the SAME ORDER. Each line must have:
  (a) A narrative sentence citing exactly WHERE — name the specific table/section/figure
      and include a verbatim excerpt (≤15 words) that confirms the value.
  (b) [Sources: <key1> (<location>, '<verbatim excerpt>'); <key2> (<location>, '<verbatim excerpt>')]
      List EVERY source that mentions this field.
      key = exact header from 'The source - <key>:' blocks in the text (e.g. NCBI_biosample, a URL, user_uploaded_file)
      location = table/section name (e.g. 'Table 3', 'Abstract', 'species description', 'Supplementary Table S1')
      verbatim excerpt = ≤15-word exact quote confirming the value
  (c) [Conflict: <describe any disagreement between sources, or 'none'>]
  (d) [ID-match: true|false] -- true ONLY if this value was confirmed by finding this sample's own numeric ID/identifier as a row in a numbered table (per the PRIORITY RULE above, when applicable); false if the value instead came from general prose, a topical description, or any source that doesn't tie the value to this sample's specific ID. For fields where no such per-sample table lookup is relevant (e.g. a value read directly from this sample's own BioSample/SRA record), output true.
Example for 3 fields (country, modern/ancient, group_status) -- placeholder values only:
  Spain | modern | ConditionX ##CONFLICT: source_A=GroupA, source_B=ConditionX
  The geo_loc_name attribute is 'Spain: Region' and subjects were recruited locally. [Sources: NCBI_biosample (geo_loc_name attribute, 'Spain: Region'); https://doi.org/10.9999/example (Methods section, 'recruited in Region')] [Conflict: none] [ID-match: true]
  Subjects were described as living participants enrolled in 2020 per the Methods section. [Sources: https://doi.org/10.9999/example (Methods section, 'living participants enrolled in 2020')] [Conflict: none] [ID-match: false]
  Supplementary Table S2 row 7 lists this subject's numeric ID as matching group ConditionX. [Sources: https://doi.org/10.9999/example (Supplementary Table S2 row 7, 'subject 7, group ConditionX')] [Conflict: none] [ID-match: true]

Text Snippets:
[given_context_for_llm]